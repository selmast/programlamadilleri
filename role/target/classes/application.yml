spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/role
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      #ddl-auto: create
    show-sql: true
  jackson:
    default-property-inclusion: non_null
    serialization.FAIL_ON_EMPTY_BEANS: false

permission:
  service: http://localhost:8082

server:
  port: 8083
